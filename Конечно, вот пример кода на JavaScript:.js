Конечно, вот пример кода на JavaScript:

```javascript
// Пример функции на JavaScript, которая возвращает сумму двух чисел
function sum(a, b) {
  return a + b;
}

// Пример использования функции
var result = sum(2, 3);
console.log(result); // Выведет 5 в консоль
```

Этот код определяет функцию `sum`, которая принимает два параметра и возвращает их сумму. Затем функция вызывается с аргументами `2` и `3`, и результат (число `5`) присваивается переменной `result`. Наконец, результат выводится в консоль с помощью метода `console.log()`.